# Read by `docker compose build` (root .env, picked up automatically by
# compose) to bake a tenant key into the production frontend image.
# For local non-Docker `npm run dev`, see audit_intelligence/frontend/.env.example
# instead.
VITE_TENANT_KEY=local-dev-default-key
